pluginManagement {
    repositories {
        mavenLocal()
        gradlePluginPortal()
        mavenCentral()
        maven {
            url "https://repo.imanity.dev/imanity-libraries"
        }
    }
}

rootProject.name = "craftcore"
